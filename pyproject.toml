[project]
name = "is-burger"
version = "0.1.1"
description = "Burger detection"
readme = "README.md"
authors = [
    { name = "ShackledMars261" }
]
requires-python = ">=3.13"
dependencies = []
license = "MIT"
license-files = ["LICENSE"]

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"
